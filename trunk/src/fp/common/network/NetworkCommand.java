package fp.common.network;

public enum NetworkCommand {
	getActivities, getActivity, markRead, markUnread, createActivity,
	returnActivities, returnEmployees, getEmployees, getCredentials, returnCredentials
}
